import React from 'react';
import Loader from '../../../Components/Loader/Loader';

const Home = () => {
    return (
        <div>
            <p>This is home!</p>
            <Loader/>
        </div>
    );
};

export default Home;